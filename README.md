# Custom Ubuntu ISO Builder 🛠️

This workspace contains tools and scripts to create a customized Ubuntu ISO (Remastering).

## Steps:
1. Place your base Ubuntu ISO in `/home/user/ubuntu-builder/ubuntu.iso` (or download one).
2. Run `./build.sh` to extract, customize, and rebuild the ISO.
